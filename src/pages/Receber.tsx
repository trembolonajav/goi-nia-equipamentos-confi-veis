import{useEffect,useState}from"react";import{useNavigate}from"react-router-dom";import{PageHeader,Tag,useToast}from"../components/ui";import{brl}from"../lib/calc";import{atendimentoApi,type CobrancaApi}from"../lib/api";
export default function Receber(){const[lista,setLista]=useState<CobrancaApi[]>([]),[erro,setErro]=useState(''),[selecionada,setSelecionada]=useState<CobrancaApi|null>(null),[valor,setValor]=useState(''),[forma,setForma]=useState('Pix'),[salvando,setSalvando]=useState(false);const nav=useNavigate();const{toast}=useToast();const carregar=()=>atendimentoApi.cobrancas().then(setLista).catch(()=>setErro('Não foi possível carregar as cobranças.'));useEffect(()=>{void carregar()},[]);function abrir(c:CobrancaApi){setSelecionada(c);setValor(String(c.saldo));setForma('Pix')}async function receber(){if(!selecionada)return;const numero=Number(valor.replace(',','.'));if(!Number.isFinite(numero)||numero<=0)return toast('Informe um valor válido.');setSalvando(true);try{await atendimentoApi.receberCobranca(selecionada.id,numero,forma);toast('Recebimento registrado e lançado no caixa.');setSelecionada(null);await carregar()}catch(e){toast(e instanceof Error?e.message:'Não foi possível registrar o recebimento.')}finally{setSalvando(false)}}const vencido=lista.filter(c=>c.status==='VENCIDA').reduce((a,c)=>a+Number(c.saldo),0),aberto=lista.filter(c=>c.status!=='PAGA').reduce((a,c)=>a+Number(c.saldo),0),recebido=lista.reduce((a,c)=>a+Number(c.recebido),0);return <main className="page" style={{maxWidth:1300}}><PageHeader title="Contas a receber" sub="Cobranças geradas pelos contratos. Pagamentos parciais preservam o saldo pendente." action={<button className="btn btn-ghost" onClick={carregar}>Atualizar</button>}/><div className="grid" style={{gridTemplateColumns:'repeat(3,1fr)',marginTop:20}}>{[['Vencido',vencido,'var(--red)'],['Em aberto',aberto,'var(--yellow)'],['Recebido',recebido,'var(--green)']].map(k=><div className="card-tight" key={String(k[0])}><div className="uplabel">{k[0]}</div><div className="num" style={{fontSize:25,color:String(k[2])}}>{brl.format(Number(k[1]))}</div></div>)}</div><div className="list-head" style={{marginTop:24,gridTemplateColumns:'100px 1.3fr 130px 120px 130px 160px'}}><span>Cobrança</span><span>Cliente</span><span>Vencimento</span><span>Situação</span><span>Saldo</span><span>Ação</span></div><div className="list">{lista.map(c=><div key={c.id} className="list-row" style={{gridTemplateColumns:'100px 1.3fr 130px 120px 130px 160px',cursor:'default'}}><button className="link-back mono orange" onClick={()=>nav(`/app/contratos/${c.contrato}`)}>CB-{String(c.id).padStart(5,'0')}</button><span><strong>{c.cliente}</strong><small style={{display:'block',color:'var(--muted)'}}>{c.descricao} · {c.contrato}</small></span><span>{c.vencimento.split('-').reverse().join('/')}</span><Tag cor={c.status==='PAGA'?'var(--green)':c.status==='VENCIDA'?'var(--red)':'var(--yellow)'}>{c.status}</Tag><span className="num">{brl.format(Number(c.saldo))}</span><span>{c.status!=='PAGA'&&<button className="btn btn-primary btn-sm" onClick={()=>abrir(c)}>Registrar recebimento</button>}</span></div>)}{!erro&&!lista.length&&<div className="empty">Nenhuma cobrança real.</div>}{erro&&<div className="empty" style={{color:'var(--red)'}}>{erro}</div>}</div>{selecionada&&<div className="document-modal"><div className="issue-modal"><header><div><div className="uplabel">CB-{String(selecionada.id).padStart(5,'0')}</div><h2 className="h2">Registrar recebimento</h2></div><button className="document-modal-close" onClick={()=>setSelecionada(null)}>×</button></header><div className="card-tight" style={{marginBottom:16}}><strong>{selecionada.cliente}</strong><small style={{display:'block',color:'var(--muted)'}}>Saldo pendente: {brl.format(Number(selecionada.saldo))}</small></div><div className="form-grid"><label className="field"><span>Valor recebido</span><input className="input" value={valor} onChange={e=>setValor(e.target.value)} inputMode="decimal"/></label><label className="field"><span>Forma de pagamento</span><select className="select" value={forma} onChange={e=>setForma(e.target.value)}>{['Pix','Dinheiro','Boleto','Cartão de crédito','Cartão de débito','Transferência'].map(f=><option key={f}>{f}</option>)}</select></label></div><p className="muted" style={{fontSize:13}}>Ao confirmar, o valor também será lançado automaticamente como entrada no caixa.</p><div className="row" style={{justifyContent:'flex-end'}}><button className="btn btn-ghost" onClick={()=>setSelecionada(null)}>Cancelar</button><button className="btn btn-primary" disabled={salvando} onClick={receber}>{salvando?'Registrando...':'Confirmar recebimento'}</button></div></div></div>}</main>}

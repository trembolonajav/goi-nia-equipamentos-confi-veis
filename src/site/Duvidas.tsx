import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { FAQ } from "../data/catalogo";
import { openWhatsApp, WHATSAPP_DISPLAY } from "../lib/whatsapp";
export default function Duvidas() { const nav = useNavigate(); const [aberta,setAberta] = useState(0); return <main className="site-wrap" style={{ padding: "40px 0 80px" }}><div className="row" style={{ gap:8 }}><button className="link-back" onClick={() => nav("/site")}>Início</button><span>/</span><span className="muted">Dúvidas</span></div><h1 className="h1" style={{ marginTop: 12 }}>Dúvidas frequentes</h1><p className="muted">Se não encontrar sua resposta, fale diretamente com a LOCAGO.</p><div className="site-cols-2" style={{ marginTop:32, display:"grid", gridTemplateColumns:"1.4fr .6fr", gap:48 }}><div style={{ borderTop:"1px solid var(--border)" }}>{FAQ.map((f,i)=><div key={f.q}><button onClick={()=>setAberta(aberta===i?-1:i)} style={{ width:"100%", minHeight:56, background:"none", border:0, borderBottom:"1px solid var(--border)", color:aberta===i?"var(--orange)":"var(--text)", textAlign:"left", fontWeight:600, cursor:"pointer" }}>{f.q}</button>{aberta===i&&<p className="muted" style={{ padding:"0 0 20px", borderBottom:"1px solid var(--border)" }}>{f.a}</p>}</div>)}</div><aside className="card" style={{ height:"fit-content" }}><h2 className="h2">Falar com a gente</h2><p className="muted">{WHATSAPP_DISPLAY}<br/>Atendimento pelo WhatsApp</p><button className="btn btn-primary btn-block" onClick={()=>openWhatsApp("Olá! Tenho uma dúvida sobre a locação de equipamentos.")}>Perguntar no WhatsApp</button></aside></div></main>; }
